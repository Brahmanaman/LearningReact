# Custom Hooks

Custom hooks always start with use prefix, custom hooks ki help se hum single responsibilty type component bna sakte hai.
In my case I have created a custom hook to fetch the data from api
